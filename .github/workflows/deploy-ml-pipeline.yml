name: Deploy ML Pipeline

on:
  push:
    branches: [main]
    paths:
      - 'pipelines/ml/**'
  workflow_dispatch:
    inputs:
      run_pipeline:
        description: 'Submit pipeline run after deploy'
        type: boolean
        default: true

env:
  PROJECT_ID: data-feeder-lcd
  REGION: australia-southeast1
  SERVICE_ACCOUNT: sa-ml-v@data-feeder-lcd.iam.gserviceaccount.com
  GCS_BUCKET: gs://data-feeder-lcd-ml-artifacts

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    name: Deploy ML Pipeline
    runs-on: ubuntu-latest
    environment: prod

    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud (WIF)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        working-directory: pipelines/ml
        run: pip install -r requirements.txt

      - name: Create BigQuery feature views
        run: |
          bq query \
            --project_id=${{ env.PROJECT_ID }} \
            --use_legacy_sql=false \
            < pipelines/ml/sql/player_features_view.sql
          bq query \
            --project_id=${{ env.PROJECT_ID }} \
            --use_legacy_sql=false \
            < pipelines/ml/sql/team_features_view.sql
          bq query \
            --project_id=${{ env.PROJECT_ID }} \
            --use_legacy_sql=false \
            < pipelines/ml/sql/f1_driver_features_view.sql
          bq query \
            --project_id=${{ env.PROJECT_ID }} \
            --use_legacy_sql=false \
            < pipelines/ml/sql/f1_constructor_features_view.sql
          bq query \
            --project_id=${{ env.PROJECT_ID }} \
            --use_legacy_sql=false \
            < pipelines/ml/sql/f1_pitstop_features_view.sql
          bq query \
            --project_id=${{ env.PROJECT_ID }} \
            --use_legacy_sql=false \
            < pipelines/ml/sql/nrl_team_features_view.sql
          bq query \
            --project_id=${{ env.PROJECT_ID }} \
            --use_legacy_sql=false \
            < pipelines/ml/sql/nrl_match_features_view.sql
          bq query \
            --project_id=${{ env.PROJECT_ID }} \
            --use_legacy_sql=false \
            < pipelines/ml/sql/nrl_coaching_features_view.sql

      - name: Create pipeline_runs table
        run: |
          bq query \
            --project_id=${{ env.PROJECT_ID }} \
            --use_legacy_sql=false \
            < pipelines/ml/sql/pipeline_runs_table.sql

      # --- Clustering pipeline ---
      - name: Compile clustering pipeline
        working-directory: pipelines/ml
        run: python pipeline.py

      - name: Upload clustering pipeline to GCS
        working-directory: pipelines/ml
        run: gsutil cp pipeline.json ${{ env.GCS_BUCKET }}/pipelines/player-role-clustering/pipeline.json

      - name: Submit clustering pipeline
        if: (github.event_name == 'workflow_dispatch' && inputs.run_pipeline) || github.event_name == 'push'
        working-directory: pipelines/ml
        run: |
          python -c "
          from google.cloud import aiplatform
          aiplatform.init(project='${{ env.PROJECT_ID }}', location='${{ env.REGION }}')
          job = aiplatform.PipelineJob(
              display_name='player-role-clustering-$(date +%Y%m%d-%H%M)',
              template_path='pipeline.json',
              pipeline_root='${{ env.GCS_BUCKET }}/pipeline-runs',
              parameter_values={'project_id': '${{ env.PROJECT_ID }}', 'region': '${{ env.REGION }}'},
          )
          job.submit(service_account='${{ env.SERVICE_ACCOUNT }}')
          print(f'Clustering pipeline submitted: {job.resource_name}')
          "

      # --- Anomaly detection pipeline ---
      - name: Compile anomaly pipeline
        working-directory: pipelines/ml
        run: python anomaly_pipeline.py

      - name: Upload anomaly pipeline to GCS
        working-directory: pipelines/ml
        run: gsutil cp anomaly_pipeline.json ${{ env.GCS_BUCKET }}/pipelines/player-anomaly-detection/pipeline.json

      - name: Submit anomaly pipeline
        if: (github.event_name == 'workflow_dispatch' && inputs.run_pipeline) || github.event_name == 'push'
        working-directory: pipelines/ml
        run: |
          python -c "
          from google.cloud import aiplatform
          aiplatform.init(project='${{ env.PROJECT_ID }}', location='${{ env.REGION }}')
          job = aiplatform.PipelineJob(
              display_name='player-anomaly-detection-$(date +%Y%m%d-%H%M)',
              template_path='anomaly_pipeline.json',
              pipeline_root='${{ env.GCS_BUCKET }}/pipeline-runs',
              parameter_values={'project_id': '${{ env.PROJECT_ID }}', 'region': '${{ env.REGION }}'},
          )
          job.submit(service_account='${{ env.SERVICE_ACCOUNT }}')
          print(f'Anomaly pipeline submitted: {job.resource_name}')
          "

      # --- Rating prediction pipeline ---
      - name: Compile rating pipeline
        working-directory: pipelines/ml
        run: python rating_pipeline.py

      - name: Upload rating pipeline to GCS
        working-directory: pipelines/ml
        run: gsutil cp rating_pipeline.json ${{ env.GCS_BUCKET }}/pipelines/player-rating-prediction/pipeline.json

      - name: Submit rating pipeline
        if: (github.event_name == 'workflow_dispatch' && inputs.run_pipeline) || github.event_name == 'push'
        working-directory: pipelines/ml
        run: |
          python -c "
          from google.cloud import aiplatform
          aiplatform.init(project='${{ env.PROJECT_ID }}', location='${{ env.REGION }}')
          job = aiplatform.PipelineJob(
              display_name='player-rating-prediction-$(date +%Y%m%d-%H%M)',
              template_path='rating_pipeline.json',
              pipeline_root='${{ env.GCS_BUCKET }}/pipeline-runs',
              parameter_values={'project_id': '${{ env.PROJECT_ID }}', 'region': '${{ env.REGION }}'},
          )
          job.submit(service_account='${{ env.SERVICE_ACCOUNT }}')
          print(f'Rating pipeline submitted: {job.resource_name}')
          "

      # --- Team win predictor pipeline ---
      - name: Compile team win predictor pipeline
        working-directory: pipelines/ml
        run: python team_win_predictor_pipeline.py

      - name: Upload team win predictor pipeline to GCS
        working-directory: pipelines/ml
        run: gsutil cp team_win_predictor_pipeline.json ${{ env.GCS_BUCKET }}/pipelines/team-win-predictor/pipeline.json

      - name: Submit team win predictor pipeline
        if: (github.event_name == 'workflow_dispatch' && inputs.run_pipeline) || github.event_name == 'push'
        working-directory: pipelines/ml
        run: |
          python -c "
          from google.cloud import aiplatform
          aiplatform.init(project='${{ env.PROJECT_ID }}', location='${{ env.REGION }}')
          job = aiplatform.PipelineJob(
              display_name='team-win-predictor-$(date +%Y%m%d-%H%M)',
              template_path='team_win_predictor_pipeline.json',
              pipeline_root='${{ env.GCS_BUCKET }}/pipeline-runs',
              parameter_values={'project_id': '${{ env.PROJECT_ID }}', 'region': '${{ env.REGION }}'},
          )
          job.submit(service_account='${{ env.SERVICE_ACCOUNT }}')
          print(f'Team win predictor submitted: {job.resource_name}')
          "

      # --- Team archetype clustering pipeline ---
      - name: Compile team archetype pipeline
        working-directory: pipelines/ml
        run: python team_archetype_pipeline.py

      - name: Upload team archetype pipeline to GCS
        working-directory: pipelines/ml
        run: gsutil cp team_archetype_pipeline.json ${{ env.GCS_BUCKET }}/pipelines/team-archetype-clustering/pipeline.json

      - name: Submit team archetype pipeline
        if: (github.event_name == 'workflow_dispatch' && inputs.run_pipeline) || github.event_name == 'push'
        working-directory: pipelines/ml
        run: |
          python -c "
          from google.cloud import aiplatform
          aiplatform.init(project='${{ env.PROJECT_ID }}', location='${{ env.REGION }}')
          job = aiplatform.PipelineJob(
              display_name='team-archetype-clustering-$(date +%Y%m%d-%H%M)',
              template_path='team_archetype_pipeline.json',
              pipeline_root='${{ env.GCS_BUCKET }}/pipeline-runs',
              parameter_values={'project_id': '${{ env.PROJECT_ID }}', 'region': '${{ env.REGION }}'},
          )
          job.submit(service_account='${{ env.SERVICE_ACCOUNT }}')
          print(f'Team archetype clustering submitted: {job.resource_name}')
          "

      # --- Team optimal profile pipeline ---
      - name: Compile team optimal profile pipeline
        working-directory: pipelines/ml
        run: python team_optimal_profile_pipeline.py

      - name: Upload team optimal profile pipeline to GCS
        working-directory: pipelines/ml
        run: gsutil cp team_optimal_profile_pipeline.json ${{ env.GCS_BUCKET }}/pipelines/team-optimal-profile/pipeline.json

      - name: Submit team optimal profile pipeline
        if: (github.event_name == 'workflow_dispatch' && inputs.run_pipeline) || github.event_name == 'push'
        working-directory: pipelines/ml
        run: |
          python -c "
          from google.cloud import aiplatform
          aiplatform.init(project='${{ env.PROJECT_ID }}', location='${{ env.REGION }}')
          job = aiplatform.PipelineJob(
              display_name='team-optimal-profile-$(date +%Y%m%d-%H%M)',
              template_path='team_optimal_profile_pipeline.json',
              pipeline_root='${{ env.GCS_BUCKET }}/pipeline-runs',
              parameter_values={'project_id': '${{ env.PROJECT_ID }}', 'region': '${{ env.REGION }}'},
          )
          job.submit(service_account='${{ env.SERVICE_ACCOUNT }}')
          print(f'Team optimal profile submitted: {job.resource_name}')
          "

      # --- Team-to-player bridge pipeline ---
      - name: Compile team player bridge pipeline
        working-directory: pipelines/ml
        run: python team_player_bridge_pipeline.py

      - name: Upload team player bridge pipeline to GCS
        working-directory: pipelines/ml
        run: gsutil cp team_player_bridge_pipeline.json ${{ env.GCS_BUCKET }}/pipelines/team-player-bridge/pipeline.json

      - name: Submit team player bridge pipeline
        if: (github.event_name == 'workflow_dispatch' && inputs.run_pipeline) || github.event_name == 'push'
        working-directory: pipelines/ml
        run: |
          python -c "
          from google.cloud import aiplatform
          aiplatform.init(project='${{ env.PROJECT_ID }}', location='${{ env.REGION }}')
          job = aiplatform.PipelineJob(
              display_name='team-player-bridge-$(date +%Y%m%d-%H%M)',
              template_path='team_player_bridge_pipeline.json',
              pipeline_root='${{ env.GCS_BUCKET }}/pipeline-runs',
              parameter_values={'project_id': '${{ env.PROJECT_ID }}', 'region': '${{ env.REGION }}'},
          )
          job.submit(service_account='${{ env.SERVICE_ACCOUNT }}')
          print(f'Team player bridge submitted: {job.resource_name}')
          "

      # --- NFL team analysis pipeline ---
      - name: Compile NFL team analysis pipeline
        working-directory: pipelines/ml
        run: python nfl_team_analysis_pipeline.py

      - name: Upload NFL team analysis pipeline to GCS
        working-directory: pipelines/ml
        run: gsutil cp nfl_team_analysis_pipeline.json ${{ env.GCS_BUCKET }}/pipelines/nfl-team-analysis/pipeline.json

      - name: Submit NFL team analysis pipeline
        if: (github.event_name == 'workflow_dispatch' && inputs.run_pipeline) || github.event_name == 'push'
        working-directory: pipelines/ml
        run: |
          python -c "
          from google.cloud import aiplatform
          aiplatform.init(project='${{ env.PROJECT_ID }}', location='${{ env.REGION }}')
          job = aiplatform.PipelineJob(
              display_name='nfl-team-analysis-$(date +%Y%m%d-%H%M)',
              template_path='nfl_team_analysis_pipeline.json',
              pipeline_root='${{ env.GCS_BUCKET }}/pipeline-runs',
              parameter_values={'project_id': '${{ env.PROJECT_ID }}', 'region': '${{ env.REGION }}'},
          )
          job.submit(service_account='${{ env.SERVICE_ACCOUNT }}')
          print(f'NFL team analysis submitted: {job.resource_name}')
          "

      # --- F1 Driver Predictor pipeline ---
      - name: Compile F1 driver predictor pipeline
        working-directory: pipelines/ml
        run: python f1_driver_predictor_pipeline.py

      - name: Upload F1 driver predictor pipeline to GCS
        working-directory: pipelines/ml
        run: gsutil cp f1_driver_predictor_pipeline.json ${{ env.GCS_BUCKET }}/pipelines/f1-driver-predictor/pipeline.json

      - name: Submit F1 driver predictor pipeline
        if: (github.event_name == 'workflow_dispatch' && inputs.run_pipeline) || github.event_name == 'push'
        working-directory: pipelines/ml
        run: |
          python -c "
          from google.cloud import aiplatform
          aiplatform.init(project='${{ env.PROJECT_ID }}', location='${{ env.REGION }}')
          job = aiplatform.PipelineJob(
              display_name='f1-driver-predictor-$(date +%Y%m%d-%H%M)',
              template_path='f1_driver_predictor_pipeline.json',
              pipeline_root='${{ env.GCS_BUCKET }}/pipeline-runs',
              parameter_values={'project_id': '${{ env.PROJECT_ID }}', 'region': '${{ env.REGION }}'},
          )
          job.submit(service_account='${{ env.SERVICE_ACCOUNT }}')
          print(f'F1 driver predictor submitted: {job.resource_name}')
          "

      # --- F1 Constructor Dominance pipeline ---
      - name: Compile F1 constructor dominance pipeline
        working-directory: pipelines/ml
        run: python f1_constructor_dominance_pipeline.py

      - name: Upload F1 constructor dominance pipeline to GCS
        working-directory: pipelines/ml
        run: gsutil cp f1_constructor_dominance_pipeline.json ${{ env.GCS_BUCKET }}/pipelines/f1-constructor-dominance/pipeline.json

      - name: Submit F1 constructor dominance pipeline
        if: (github.event_name == 'workflow_dispatch' && inputs.run_pipeline) || github.event_name == 'push'
        working-directory: pipelines/ml
        run: |
          python -c "
          from google.cloud import aiplatform
          aiplatform.init(project='${{ env.PROJECT_ID }}', location='${{ env.REGION }}')
          job = aiplatform.PipelineJob(
              display_name='f1-constructor-dominance-$(date +%Y%m%d-%H%M)',
              template_path='f1_constructor_dominance_pipeline.json',
              pipeline_root='${{ env.GCS_BUCKET }}/pipeline-runs',
              parameter_values={'project_id': '${{ env.PROJECT_ID }}', 'region': '${{ env.REGION }}'},
          )
          job.submit(service_account='${{ env.SERVICE_ACCOUNT }}')
          print(f'F1 constructor dominance submitted: {job.resource_name}')
          "

      # --- F1 Pit Stop Strategy pipeline ---
      - name: Compile F1 pitstop strategy pipeline
        working-directory: pipelines/ml
        run: python f1_pitstop_strategy_pipeline.py

      - name: Upload F1 pitstop strategy pipeline to GCS
        working-directory: pipelines/ml
        run: gsutil cp f1_pitstop_strategy_pipeline.json ${{ env.GCS_BUCKET }}/pipelines/f1-pitstop-strategy/pipeline.json

      - name: Submit F1 pitstop strategy pipeline
        if: (github.event_name == 'workflow_dispatch' && inputs.run_pipeline) || github.event_name == 'push'
        working-directory: pipelines/ml
        run: |
          python -c "
          from google.cloud import aiplatform
          aiplatform.init(project='${{ env.PROJECT_ID }}', location='${{ env.REGION }}')
          job = aiplatform.PipelineJob(
              display_name='f1-pitstop-strategy-$(date +%Y%m%d-%H%M)',
              template_path='f1_pitstop_strategy_pipeline.json',
              pipeline_root='${{ env.GCS_BUCKET }}/pipeline-runs',
              parameter_values={'project_id': '${{ env.PROJECT_ID }}', 'region': '${{ env.REGION }}'},
          )
          job.submit(service_account='${{ env.SERVICE_ACCOUNT }}')
          print(f'F1 pitstop strategy submitted: {job.resource_name}')
          "

      # --- NRL Team Dominance pipeline ---
      - name: Compile NRL team dominance pipeline
        working-directory: pipelines/ml
        run: python nrl_team_dominance_pipeline.py

      - name: Upload NRL team dominance pipeline to GCS
        working-directory: pipelines/ml
        run: gsutil cp nrl_team_dominance_pipeline.json ${{ env.GCS_BUCKET }}/pipelines/nrl-team-dominance/pipeline.json

      - name: Submit NRL team dominance pipeline
        if: (github.event_name == 'workflow_dispatch' && inputs.run_pipeline) || github.event_name == 'push'
        working-directory: pipelines/ml
        run: |
          python -c "
          from google.cloud import aiplatform
          aiplatform.init(project='${{ env.PROJECT_ID }}', location='${{ env.REGION }}')
          job = aiplatform.PipelineJob(
              display_name='nrl-team-dominance-$(date +%Y%m%d-%H%M)',
              template_path='nrl_team_dominance_pipeline.json',
              pipeline_root='${{ env.GCS_BUCKET }}/pipeline-runs',
              parameter_values={'project_id': '${{ env.PROJECT_ID }}', 'region': '${{ env.REGION }}'},
          )
          job.submit(service_account='${{ env.SERVICE_ACCOUNT }}')
          print(f'NRL team dominance submitted: {job.resource_name}')
          "

      # --- NRL Match Predictor pipeline ---
      - name: Compile NRL match predictor pipeline
        working-directory: pipelines/ml
        run: python nrl_match_predictor_pipeline.py

      - name: Upload NRL match predictor pipeline to GCS
        working-directory: pipelines/ml
        run: gsutil cp nrl_match_predictor_pipeline.json ${{ env.GCS_BUCKET }}/pipelines/nrl-match-predictor/pipeline.json

      - name: Submit NRL match predictor pipeline
        if: (github.event_name == 'workflow_dispatch' && inputs.run_pipeline) || github.event_name == 'push'
        working-directory: pipelines/ml
        run: |
          python -c "
          from google.cloud import aiplatform
          aiplatform.init(project='${{ env.PROJECT_ID }}', location='${{ env.REGION }}')
          job = aiplatform.PipelineJob(
              display_name='nrl-match-predictor-$(date +%Y%m%d-%H%M)',
              template_path='nrl_match_predictor_pipeline.json',
              pipeline_root='${{ env.GCS_BUCKET }}/pipeline-runs',
              parameter_values={'project_id': '${{ env.PROJECT_ID }}', 'region': '${{ env.REGION }}'},
          )
          job.submit(service_account='${{ env.SERVICE_ACCOUNT }}')
          print(f'NRL match predictor submitted: {job.resource_name}')
          "

      # --- NRL Coaching Insights pipeline ---
      - name: Compile NRL coaching insights pipeline
        working-directory: pipelines/ml
        run: python nrl_coaching_insights_pipeline.py

      - name: Upload NRL coaching insights pipeline to GCS
        working-directory: pipelines/ml
        run: gsutil cp nrl_coaching_insights_pipeline.json ${{ env.GCS_BUCKET }}/pipelines/nrl-coaching-insights/pipeline.json

      - name: Submit NRL coaching insights pipeline
        if: (github.event_name == 'workflow_dispatch' && inputs.run_pipeline) || github.event_name == 'push'
        working-directory: pipelines/ml
        run: |
          python -c "
          from google.cloud import aiplatform
          aiplatform.init(project='${{ env.PROJECT_ID }}', location='${{ env.REGION }}')
          job = aiplatform.PipelineJob(
              display_name='nrl-coaching-insights-$(date +%Y%m%d-%H%M)',
              template_path='nrl_coaching_insights_pipeline.json',
              pipeline_root='${{ env.GCS_BUCKET }}/pipeline-runs',
              parameter_values={'project_id': '${{ env.PROJECT_ID }}', 'region': '${{ env.REGION }}'},
          )
          job.submit(service_account='${{ env.SERVICE_ACCOUNT }}')
          print(f'NRL coaching insights submitted: {job.resource_name}')
          "

      - name: Show pipeline info
        run: |
          echo "### ML Pipelines Deployed" >> "$GITHUB_STEP_SUMMARY"
          echo "#### Player Pipelines" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Clustering**: \`player-role-clustering\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Anomaly Detection**: \`player-anomaly-detection\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Rating Prediction**: \`player-rating-prediction\`" >> "$GITHUB_STEP_SUMMARY"
          echo "#### NFL Team Pipelines" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Win Predictor**: \`team-win-predictor\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Archetype Clustering**: \`team-archetype-clustering\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Optimal Profile**: \`team-optimal-profile\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Player Bridge**: \`team-player-bridge\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- **NFL Team Analysis**: \`nfl-team-analysis\`" >> "$GITHUB_STEP_SUMMARY"
          echo "#### NRL Pipelines" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Team Dominance**: \`nrl-team-dominance\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Match Predictor**: \`nrl-match-predictor\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Coaching Insights**: \`nrl-coaching-insights\`" >> "$GITHUB_STEP_SUMMARY"
          echo "#### F1 Pipelines" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Driver Predictor**: \`f1-driver-predictor\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Constructor Dominance**: \`f1-constructor-dominance\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Pit Stop Strategy**: \`f1-pitstop-strategy\`" >> "$GITHUB_STEP_SUMMARY"
